<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.interaction.mapper.SmartVillageCommentMapper">

    <resultMap id="BaseResultMap" type="org.jeecg.modules.interaction.domain.SmartVillageComment">
            <id property="id" column="id" jdbcType="VARCHAR"/>
            <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="sysOrgCode" column="sys_org_code" jdbcType="VARCHAR"/>
            <result property="content" column="content" jdbcType="VARCHAR"/>
            <result property="topicId" column="topic_id" jdbcType="VARCHAR"/>
            <result property="userId" column="user_id" jdbcType="VARCHAR"/>
            <result property="inTime" column="in_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,create_by,create_time,
        update_by,update_time,sys_org_code,
        content,topic_id,user_id,
        in_time
    </sql>
    <select id="getCommentList" parameterType="Object" resultType="org.jeecg.modules.interaction.vo.CommentVo">
        SELECT
            tt.title,
            c.content,
            c.create_by,
            c.create_time
        FROM
            (
                SELECT
                    t.*
                FROM
                    smart_village_topic t
                        LEFT JOIN sys_user_depart u ON t.user_id = u.user_id
                WHERE
                    u.dep_id = #{departId}
                  AND t.status = 1
            ) tt
                LEFT JOIN smart_village_comment c ON tt.id = c.topic_id
        WHERE
            c.status = '1'
        order by c.create_time desc
    </select>
</mapper>
